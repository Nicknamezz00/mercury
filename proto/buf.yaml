version: v1
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  except:
    - ENUM_VALUE_PREFIX
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
deps:
  - buf.build/googleapis/googleapis
  - bud.build/grpc-ecosystem/grpc-gateway